@page
@model eShop.Pages.ProductsModel
@{
}

<div class="container">
    <div class="row g-5">
        @foreach (var prod in Model.Products)
        {
            <partial name="_Product" model="prod" />
        }
    </div>
</div>
